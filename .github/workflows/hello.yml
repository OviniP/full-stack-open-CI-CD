name: Hello World!

on:
  push:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: say hello
        run: |
          echo "Hello World!"
      - name: now it is
        run: | 
          date
      - name: Directory content
        run: |
          ls -l
